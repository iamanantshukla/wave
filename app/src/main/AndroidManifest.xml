<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev334.wave">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wave">

        <activity android:name=".ChooseInterest"/>
        <activity android:name=".ChooseOrganisation" />
        <activity android:name=".CreateProfile" />
        <activity android:name=".LoginHome" />
        <activity android:name=".EmailLogin" />
        <activity android:name=".SplashScreen"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".VideoCall">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>